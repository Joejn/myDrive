import { ForbiddenFilenameDirective } from './forbidden-filename.directive';

describe('ForbiddenFilenameDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenFilenameDirective();
    expect(directive).toBeTruthy();
  });
});
